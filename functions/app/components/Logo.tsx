import logo from '../assets/based.svg'

export const Logo = () => {
  return (
    <img
      style={{
        marginLeft: 'auto',
        marginRight: 'auto',
      }}
      alt="Based.io"
      src={logo}
    />
  )
}
